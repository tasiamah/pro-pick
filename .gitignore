# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv*/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.db
*.sqlite3

# Env / secrets
.env
.env.*
!.env.example

# ML artifacts (runtime-trained models are local-only)
backend/app/ml/*.pkl
# ...except the shipped baseline that ships with the deploy so a fresh instance
# serves real predictions without training on startup.
!backend/app/ml/pretrained_model.pkl

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS / editors
.DS_Store
.idea/
.vscode/
*.swp
